# Top-Tier Deep Dive: 2026-03-21

## Scope

Deep dive on the current best 6 chips from the live `portfolio-v3` run on March 21, 2026.

## Core Read

All six top chips are already `production_ready` on v2, but still only `beta` on v3. That matters. It means the leadership tier is not blocked by basic chip correctness anymore. The remaining gap is deeper operating truth:

- doctrine quality
- evidence balance
- contradiction resolution
- real flywheel quality over time

## The 6

### `domain-chip-startup-yc`

- v2: `98`, `production_ready`
- v3: `72.0`, `beta`
- read: flagship

Why it wins:
- `785` runs
- `124119` artifacts
- `276` vault docs
- best watchtower and strongest operating depth in the whole portfolio

What still holds it back:
- doctrine quality is only moderate for a chip this mature
- contradiction resolution is weak relative to the amount of history

### `domain-chip-xcontent`

- v2: `92`, `production_ready`
- v3: `65.6`, `beta`
- read: promising but not fully trusted

Why it matters:
- strong evidence integrity
- strong recent trajectory
- better doctrine quality than most of the portfolio

What still holds it back:
- `single_day_batch` anti-gaming flag
- no realworld_validated lane yet

### `domain-chip-pokemon-red`

- v2: `98`, `production_ready`
- v3: `62.8`, `beta`
- read: high-volume specialist

Why it matters:
- `776` runs
- `66903` artifacts
- excellent watchtower substance

What still holds it back:
- evidence balance is skewed
- contradiction rigor is too light for the amount of operating history

### `domain-chip-content`

- v2: `83`, `production_ready`
- v3: `62.0`, `beta`
- read: intellectually strongest relative to size

Why it matters:
- best doctrine quality in the top tier
- strong contradiction rigor for its size

What still holds it back:
- only `10` runs
- no realworld_validated lane
- weak watchtower substance

### `domain-chip-agentic-marketing`

- v2: `100`, `production_ready`
- v3: `60.0`, `beta`
- read: operational but drifting

Why it matters:
- meaningful artifact volume
- healthy watchtower depth
- real contradiction coverage

What still holds it back:
- trajectory is not improving
- doctrine quality is still moderate with zero causal density

### `domain-chip-spark-private`

- v2: `100`, `production_ready`
- v3: `58.0`, `beta`
- read: broad internal operating system

Why it matters:
- strong watchtower depth
- broad internal operating surface

What still holds it back:
- only `8` recorded runs
- no realworld_validated lane
- doctrine quality is still moderate

## What This Means

- `startup-yc` is the flagship reference chip.
- `xcontent` is near-leader quality, but still on probation until the anti-gaming flag clears.
- `content` is the best doctrine-quality model in the portfolio.
- `pokemon-red` proves scale, but not yet disciplined reflection at the same scale.
- `agentic-marketing` and `spark-private` are real, but they need to prove positive trajectory rather than just structural maturity.

## What I Would Do

1. Use `startup-yc` as the operating-depth benchmark.
2. Use `content` as the doctrine-quality benchmark.
3. Force `xcontent` through an anti-gaming review before promoting it as a transfer source.
4. Make `pokemon-red` invest in contradiction rigor before adding even more history.
5. Make `agentic-marketing` and `spark-private` earn more strategic weight through improved trajectory, not just more artifacts.
