# MiroFish Discovery Frontier 1000 Seed Groups

This tranche packages the first same-persona seed pool into repo-local grouped intake artifacts so broad collection can move faster.

What is included:

- `creator_media_social` seed group
- `gaming_community` seed group
- `agent_startup_productivity` seed group
- `crypto_consumer_career` seed group

Why this tranche exists:

- the frontier fast path already created the `1000`-agent diverse scaffold
- sub-agents already produced the first `60` provisional ideas
- those ideas needed to be persisted in small, reviewable artifacts instead of one oversized patch

Operating rule:

- these are seed ideas for broad intake only
- simulation remains deferred
- canonicalization and later selection decide what survives
